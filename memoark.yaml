# Memoark Configuration

# Privacy configuration
privacy:
  enabled: true
  mode: reversible  # reversible or irreversible
  redact_phone: true
  redact_id_card: true
  redact_bank_card: true
  redact_email: false
  redact_url: false
  blocked_words: []
  replacement: "[REDACTED]"

# LLM provider configuration
llm:
  provider: openai
  model: MiniMax-M2.5
  base_url: https://api.tokenflying.top
  api_key: ${OPENAI_API_KEY}

# Block builder configuration
block_builder:
  block_gap_minutes: 30  # Gap between messages to start a new block
  max_block_tokens: 4000  # Maximum tokens per block
  max_block_messages: 100  # Maximum messages per block

# Source configuration
sources:
  claude-code:
    enabled: true
  codex:
    enabled: true
  hermes:
    enabled: true
  feishu:
    enabled: true
    auth_mode: user
    app_id: cli_a94d131ca0b89bc6
    app_secret: unused-in-user-mode
    sources:
      messages:
        enabled: true
        chat_ids:
          - oc_9a29c0eaf777bf346dc27cf2b892c597

# Adapter configuration
adapters:
  file:
    enabled: false
    output_dir: ./output
  gbrain:
    enabled: false
    output_dir: ./gbrain-output

# Store (PGLite embedded PostgreSQL)
store:
  data_dir: ~/.memoark/data

# Embedding configuration
embedding:
  provider: ollama
  model: nomic-embed-text
  dimensions: 768
  # api_key: <your-api-key>  # Or set OPENAI_API_KEY env var

# Server configuration
server:
  http_port: 3927
