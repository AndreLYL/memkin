# Dependencies
node_modules/
package-lock.json
# bun.lock is committed for CI --frozen-lockfile

# Build output
dist/
web/dist/
web/node_modules/
web/bun.lock
graphify-out/

# State directory
.dbe/
.memkin/
.memkin.bak.*/

# Environment variables
.env
.env.local
.env.*.local

# Local config (may contain API keys)
memkin.yaml

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
.superpowers/

# Logs
*.log
logs/

# Extraction output
output/

# Test temp files
tests/temp/

# Local golden eval dataset (real sessions + human annotations — private, never commit).
# Only the sanitized CI fixtures under tests/fixtures/eval/ ship in the repo.
eval-data/

# PGlite in-memory filesystem artifact
:memory:/

# Local isolated worktrees
.worktrees/

# Implementation plans (managed in separate repo: AndreLYL/memoark_spec)
docs/superpowers/
dist-bin/
web/tsconfig.tsbuildinfo
