# 对比度已知例外(保留设计师原色值,用户定 v2)

设计师原色值整体保留,不为零星对比度问题改 token(避免破坏色彩关系)。以下为低于 WCAG AA 的已知点,记录但不阻断:

- `fg-subtle` #a59b8e on `bg-canvas` #faf8f5 ≈ 2.5:1 — 用于弱化的次级标签/时间戳,低于 AA 正文 4.5:1。决议:保留。
- 白字 on `accent` #c2613d ≈ 4.2:1 — 主按钮文字,略低于 AA 正文 4.5:1;在大字号/按钮场景达 ≥3:1。决议:保留。

如需达标,可在后续单独 issue 里微调 token,不在本次重设计范围。
